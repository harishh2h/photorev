{
  "name": "photorev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter @photorev/frontend dev",
    "backend:dev": "pnpm --filter @photorev/backend dev",
    "frontend:dev": "pnpm --filter @photorev/frontend dev",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "migrate:latest": "pnpm --filter @photorev/backend migrate:latest",
    "migrate:rollback": "pnpm --filter @photorev/backend migrate:rollback",
    "migrate:list": "pnpm --filter @photorev/backend migrate:list",
    "migrate:make": "pnpm --filter @photorev/backend migrate:make",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.30.2",
  "dependencies": {
    "knex": "^3.1.0"
  }
}
